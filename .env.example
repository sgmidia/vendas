# Porta do servidor (Render define automaticamente)
PORT=3000

# Caminho do banco de dados SQLite
# No Render use um disco persistente montado em /data
DB_PATH=/data/data.db

# URL da API para o frontend (deixe vazio se servido pelo mesmo servidor)
VITE_API_URL=
